## Packages
qrcode.react | Rendering WhatsApp QR codes
framer-motion | Smooth page transitions between connection states

## Notes
- Polling /api/session/status every 2 seconds using React Query
- Renders QR code raw string from API into a scannable QR code SVG
- Assumes shadcn/ui components are available in @/components/ui
